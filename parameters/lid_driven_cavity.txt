# Settings file for numsim program
# Run ./numsim lid_driven_cavity.txt

# Domain file location and scaling factor for physical domain - this needs to be specified first
domain_scaling = 10.0 # pixels per 1 physical size un10.0it. physicalsize = nCells / domain_scaling
domainFile = domains/lid_driven_cavity.txt

# Problem description
endTime = 20.0        # duration of the simulation
re = 1000             # Reynolds number
pr = 7                # Prandtl number
gX = 0.0              # external forces, set to (gX,gY) = (0,-9.81) to account for gravity
gY = -9.81

# Dirichlet boundary conditions
dirichletBottomX = 0
dirichletBottomY = 0
dirichletTopX    = 0
dirichletTopY    = 0
dirichletLeftX   = 0
dirichletLeftY   = 0
dirichletRightX  = 0
dirichletRightY  = 0

# Discretization parameters
useDonorCell = true   # if donor cell discretization should be used, possible values: true false
alpha = 0.5           # factor for donor-cell scheme, 0 is equivalent to central differences
gamma = 0.5           # factor for donor-cell for temperature
tau = 0.5             # safety factor for time step width
maximumDt = 0.1       # maximum values for time step width

# Temperature parameters
beta = 0.2            # volume expansion coefficient
initialTemp = 0   # in Kelvin, 293K = 20°C
setFixedTempBottom = true
tempBcBottom = 10    # prescribed values of tb or tn at bottom of domain
setFixedTempTop = true
tempBcTop = 0       # prescribed values of tb or tn at top of domain
setFixedTempLeft = true
tempBcLeft = 0     # prescribed values of tb or tn at left of domain
setFixedTempRight = true
tempBcRight = 0     # prescribed values of tb or tn at right of domain

# Solver parameters
pressureSolver = SOR  # which pressure solver to use, possible values: GaussSeidel SOR CG
omega = 1.6           # overrelaxation factor, only for SOR solver
epsilon = 1e-5        # tolerance for 2-norm of residual
maximumNumberOfIterations = 1e4    # maximum number of iterations in the solver
